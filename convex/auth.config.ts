const authConfig = {
    providers: [
        {
            domain: "https://deciding-badger-15.clerk.accounts.dev/",
            applicationId: "convex",
        },
    ]
}
export default authConfig;
